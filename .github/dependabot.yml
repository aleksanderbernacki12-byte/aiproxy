version: 2
updates:
  # Keeps the GitHub Actions used in .github/workflows up to date. This
  # project has zero Go module dependencies, so github-actions is the
  # only ecosystem there is anything to update here.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
